"use client";

// Navigation is now handled by bottom nav bars on each page
// This component is kept for potential future use but currently does nothing

export function Navigation() {
  // All pages now have their own bottom navigation bar
  // matching the original Flask app design
  return null;
}
